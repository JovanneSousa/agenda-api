spring:
  datasource:
    url: jdbc:postgresql://containers-us-west-123.railway.app:5432/railway
    username: postgres
    password: UlOvqnfHRYzsuBcIYCCehuXilAFDROfo
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect


management:
  endpoints:
    web:
      exposure:
        include: "*"

api:
  security:
    token:
      secret: my-very-secret-key-for-agenda-api
